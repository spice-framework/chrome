{
  "name": "spice-for-github",
  "version": "0.1.1",
  "private": true,
  "description": "Render valid Spice declaration comments as native, highlighted annotations on GitHub.",
  "type": "commonjs",
  "engines": {
    "node": "^22.13.0 || >=24"
  },
  "scripts": {
    "build": "node scripts/build.mjs && node scripts/package.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "test": "npm run test:unit && npm run test:browser",
    "test:unit": "node --test tests/unit/*.test.cjs",
    "test:browser": "node tests/browser/extension.test.mjs",
    "test:live": "node tests/browser/live-smoke.mjs",
    "validate": "node scripts/validate-extension.mjs",
    "verify": "npm run format:check && npm run lint && npm run validate && npm run test:unit && npm run build && npm run test:browser"
  },
  "devDependencies": {
    "eslint": "10.8.0",
    "playwright": "1.62.1",
    "prettier": "3.9.6"
  }
}
