import palette from "./src/palette.cjs";

export const {
  STORAGE_KEY,
  COLOR_DEFINITIONS,
  NATIVE_PREVIEW_COLORS,
  VIVID_COLORS,
  DEFAULT_SETTINGS,
  normalizeSettings,
  isColor,
} = palette;

export default palette;
